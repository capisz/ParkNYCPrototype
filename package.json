{
  "name": "pidge",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,cyan \"npm --prefix backend run dev\" \"npm --prefix web run dev -- --host 127.0.0.1\"",
    "build": "npm --prefix backend run build && npm --prefix web run build",
    "test": "npm --prefix backend test && npm --prefix web run test --if-present"
  },
  "devDependencies": { "concurrently": "^9.2.1" }
}
